Spawn
=====
A simple plugin that lets you set/warp to spawn. No dependencies.

Commands

    /spawn: warp to spawn
    /setspawn: set spawn point

Permissions

    Set spawn
    spawn.setspawn

    Teleport to spawn
    [no permission currently]